const $=s=>document.querySelector(s);const input=$('#photoInput'),drop=$('#dropZone'),workspace=$('#workspace'),canvas=$('#previewCanvas'),ctx=canvas.getContext('2d',{willReadFrequently:true});let imageData=null,analysis=null;
const checks=$('#checksList');
$('#chooseBtn').onclick=()=>input.click();$('#newPhotoBtn').onclick=()=>{input.value='';workspace.classList.add('hidden');drop.classList.remove('hidden')};input.onchange=e=>e.target.files[0]&&loadPhoto(e.target.files[0]);
['dragenter','dragover'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(ev=>drop.addEventListener(ev,e=>{e.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',e=>{const f=e.dataTransfer.files[0];if(f&&f.type.startsWith('image/'))loadPhoto(f)});
function loadPhoto(file){if(!file.type.match(/^image\/(jpeg|png|webp)$/)){alert('Please use a JPEG, PNG or WebP image.');return}const url=URL.createObjectURL(file),img=new Image();img.onload=()=>{const max=1800,scale=Math.min(1,max/Math.max(img.naturalWidth,img.naturalHeight));canvas.width=Math.round(img.naturalWidth*scale);canvas.height=Math.round(img.naturalHeight*scale);ctx.drawImage(img,0,0,canvas.width,canvas.height);imageData=ctx.getImageData(0,0,canvas.width,canvas.height);$('#fileName').textContent=file.name;$('#imageMeta').textContent=`${img.naturalWidth} × ${img.naturalHeight}px • ${(file.size/1048576).toFixed(2)} MB • ${file.type.split('/')[1].toUpperCase()}`;workspace.classList.remove('hidden');drop.classList.add('hidden');resetResults();URL.revokeObjectURL(url)};img.src=url}
function resetResults(){$('#score').textContent='—';$('#verdict').textContent='Ready to analyse';$('#verdictText').textContent='Run the checks to assess this image.';$('#sharpMetric').textContent='—';checks.innerHTML='<div class="check warn"><div class="icon">!</div><div><b>Analysis ready</b><span>Press “Run analysis” to inspect the image.</span></div><span class="status">Ready</span></div>';$('#checklist').innerHTML='';$('#histogram').getContext('2d').clearRect(0,0,700,220)}
$('#analyzeBtn').onclick=()=>{if(!imageData)return;analysis=analyse(imageData);render(analysis)};
function analyse(d){const w=d.width,h=d.height,p=d.data,n=w*h;let sum=0,sum2=0,bright=[];let hist=new Array(256).fill(0),clipped=0,shadow=0;for(let i=0;i<n;i++){const r=p[i*4],g=p[i*4+1],b=p[i*4+2],y=.2126*r+.7152*g+.0722*b;sum+=y;sum2+=y*y;hist[Math.max(0,Math.min(255,Math.round(y)))]++;if(y>=250)clipped++;if(y<=5)shadow++;bright.push(y)}const mean=sum/n,sd=Math.sqrt(Math.max(0,sum2/n-mean*mean));let lap=0,count=0;for(let y=1;y<h-1;y+=2){for(let x=1;x<w-1;x+=2){const c=luma(p,(y*w+x)*4),l=luma(p,(y*w+x-1)*4),r=luma(p,(y*w+x+1)*4),u=luma(p,((y-1)*w+x)*4),dn=luma(p,((y+1)*w+x)*4);const v=c*4-l-r-u-dn;lap+=v*v;count++}}const sharp=lap/Math.max(1,count);const clipping=(clipped+shadow)/n;const aspect=w/h;let resolution;if(w>=3000&&h>=2000)resolution='excellent';else if(w>=2000&&h>=1300)resolution='good';else if(w>=1600&&h>=1000)resolution='borderline';else resolution='low';let exposure=mean<75?'dark':mean>190?'bright':(clipped/n>.012||shadow/n>.08?'high-contrast':'good');let sharpness=sharp>850?'excellent':sharp>400?'good':sharp>180?'borderline':'soft';let noise=sd>68?'high':sd>48?'moderate':'low';let score=0;score+=resolution==='excellent'?25:resolution==='good'?22:resolution==='borderline'?15:7;score+=exposure==='good'?20:exposure==='high-contrast'?14:10;score+=sharpness==='excellent'?25:sharpness==='good'?22:sharpness==='borderline'?14:6;score+=noise==='low'?15:noise==='moderate'?10:5;score+=clipping<.035?15:clipping<.07?9:4;return{w,h,aspect,mean,clipping,shadow,sharp,noise,resolution,exposure,sharpness,score:Math.round(score),hist}}function luma(p,i){return .2126*p[i]+.7152*p[i+1]+.0722*p[i+2]}
function render(a){const items=[['Resolution',a.resolution==='excellent'||a.resolution==='good'?'good':a.resolution==='borderline'?'warn':'bad',`${a.w} × ${a.h}px`,a.resolution],['Exposure',a.exposure==='good'?'good':'warn',`Average brightness ${Math.round(a.mean)}/255`,a.exposure],['Sharpness',a.sharpness==='excellent'||a.sharpness==='good'?'good':a.sharpness==='borderline'?'warn':'bad',`Detail score ${Math.round(a.sharp)}`,a.sharpness],['Noise',a.noise==='low'?'good':a.noise==='moderate'?'warn':'bad',`Estimated tonal variation ${Math.round(a.mean>128?a.mean:128)}`,a.noise],['Clipping',a.clipping<.035?'good':a.clipping<.07?'warn':'bad',`${(a.clipping*100).toFixed(1)}% of pixels near extremes`,a.clipping<.035?'good':'review'],['Aspect ratio','good',a.aspect.toFixed(2)+' : 1','detected']];checks.innerHTML=items.map(x=>`<div class="check ${x[1]}"><div class="icon">${x[1]==='good'?'✓':x[1]==='warn'?'!':'×'}</div><div><b>${x[0]}</b><span>${x[2]}</span></div><span class="status">${x[3]}</span></div>`).join('');$('#score').textContent=a.score;$('#scoreRing').style.background=`radial-gradient(circle at center,#0a1422 58%,transparent 59%),conic-gradient(${a.score>=80?'#36d399':a.score>=60?'#fbbf24':'#fb7185'} ${a.score*3.6}deg,#18324d 0deg)`;const verdict=a.score>=85?'Strong technical candidate':a.score>=70?'Good — review before submitting':a.score>=55?'Needs attention':'Likely needs correction';$('#verdict').textContent=verdict;$('#verdictText').textContent='Use the individual checks below rather than treating the score as an acceptance guarantee.';$('#sharpMetric').textContent=Math.round(a.sharp);$('#sharpNote').textContent=a.sharpness==='excellent'||a.sharpness==='good'?'Good high-frequency detail detected. Inspect the aircraft at 100% for actual focus.':'The image may benefit from closer focus/sharpness inspection. Avoid aggressive sharpening.';$('#exposureNote').textContent=`Average brightness: ${Math.round(a.mean)}/255 • ${(a.clipping*100).toFixed(1)}% near black/white extremes.`;drawHistogram(a.hist);$('#checklist').innerHTML=[['✓','Resolution meets a useful web-photo threshold',a.resolution==='low'],['✓','Exposure does not show obvious global problems',a.exposure!=='good'],['✓','Sharpness has adequate high-frequency detail',a.sharpness==='soft'],['✓','No excessive pixel clipping detected',a.clipping>=.07],['○','Aircraft is sharp at 100% — inspect manually',false],['○','No dust spots, halos or over-processing — inspect manually',false],['○','Aircraft registration and subject are clearly identifiable — inspect manually',false],['○','Composition/cropping is acceptable — inspect manually',false]].map(x=>`<div class="checkitem ${x[2]?'warn':'ok'}">${x[0]} ${x[1]}</div>`).join('')}
function drawHistogram(hist){const c=$('#histogram'),g=c.getContext('2d');g.clearRect(0,0,c.width,c.height);g.fillStyle='#07101b';g.fillRect(0,0,c.width,c.height);const max=Math.max(...hist);g.strokeStyle='#4aa9ff';g.beginPath();hist.forEach((v,i)=>{const x=i/255*c.width,y=c.height-(v/max)*(c.height-10);i?g.lineTo(x,y):g.moveTo(x,y)});g.stroke();g.fillStyle='#6f8299';g.font='11px system-ui';g.fillText('0',5,214);g.fillText('128',c.width/2-10,214);g.fillText('255',c.width-24,214)}